\modulechapter{mod.tls.cert}

\generatesfrtable{mod.tls.cert}

% \generatebundletable{mod.tls.cert}

\moduledescription{mod.tls.cert.desc}

Module \tds{mod.tls.cert} of  subsystem 
\tdslink{sub.tls} provides functionalities for verifying X.509 certificates during the TLS handshake.

\moduleprocesses{prc.tls.cert}

\moduleprocess{prc.tls.cert.checkcert}{Prüfung des Zertifikats}

This process checks the certificate presented by the peer during the TLS
handshake.

\implementedsfr{\sfrlink{fpt_tdc.1/tls.zert}}


\moduleinterfaces{int.tls.cert}

\moduleinterfaceprovided{int.tls.cert.checkcert}

This interface triggers the certificate verification.

\moduleinterfacerequired{int.cryptsystem.algorithms.gethash}

The interface \tdslink[fq]{int.cryptsystem.algorithms.gethash} is required for
calculating the certificate hash value.





